<%= form_for(@voteaction) do |f| %>
  <% if @voteaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voteaction.errors.count, "error") %> prohibited this voteaction from being saved:</h2>

      <ul>
      <% @voteaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
