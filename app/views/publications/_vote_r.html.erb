<%= button_to "Vote", rvotes_path(rvote: {:review_id => review.id, :user_id => current_user.id } ),
              :method => :post,
              :remote => 'true',
              :class => "btn btn-orange btn-vote"%>
